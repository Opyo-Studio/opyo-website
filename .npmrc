# This tree was originally resolved by yarn 1, which ignores peer-dependency
# conflicts. npm 7+ enforces them, so `npm ci` fails on @react-three/drei@9
# (peer: @react-three/fiber@^8) against the pinned fiber@^9.
# Keeping npm's behaviour aligned with how the lockfile was actually built.
legacy-peer-deps=true
